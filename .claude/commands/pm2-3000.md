Start projects-3000 (3000) and open logs.
```bash
cd "E:\RAG\Daily Idea\本地 AI 部署评估工具\projects" && pm2 start ecosystem.config.cjs --only projects-3000 && start wt.exe -d "E:\RAG\Daily Idea\本地 AI 部署评估工具\projects" pwsh -NoExit -c "pm2 logs projects-3000"
```
